# Runtime for the API and the crawler.
fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27

# Tray icon for the local crawler. Not needed on the deployed site --
# api/requirements.txt deliberately omits them to keep the bundle small.
pystray>=0.19
pillow>=10.0

pytest>=8.0
